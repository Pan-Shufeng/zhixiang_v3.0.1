# Windows 便携包核验记录

## 2026-09-14：运行环境与启动器

- 本机：Windows NT 10.0.26200.0，64 位。尚未在其他同学电脑实测，不扩展为全部 Windows 兼容结论。
- 官方运行环境：Python 3.13.15 Windows x64 嵌入式发行包；下载地址与 SHA-256 在 `runtime_manifest.json` 中。
- 下载包实际 SHA-256 与 Python 官方发布页一致。应用内 Python 的 ssl、sqlite3、requests、pypdf 导入已通过。
- 实际附带依赖为 certifi 2026.7.22、charset-normalizer 3.5.1、idna 3.19、pypdf 6.18.1、requests 2.34.2、urllib3 2.7.0，随后补充字体解析需要的 fonttools 4.65.0；均已写入 `runtime_requirements.lock`，fontTools 导入通过。
- Python 原始许可证与依赖许可证保留。前端 React 19.3.0、React DOM 19.3.0、Scheduler 0.28.0、Lucide React 0.468.0 的原始许可收集已通过。
- 启动器只访问 127.0.0.1，等待应用的真实 health 返回；应用身份、启动记录及进程号匹配才复用。通过随机令牌请求关机，不按进程名或端口结束别的应用。
- 独立检查者在隔离的 18179 端口运行另一个测试 HTTP 服务；知向 start、stop 均拒绝操作并返回失败，原服务保持响应。没有操作演示端口 8176。

## 首个 ZIP：独立目录实测通过

初步 ZIP：2026-09-14 构建，37,734,940 字节；SHA-256 `7ba75835db10946404162110f93e96f888f72407f3319b05009f21d56597aae1`。它用于验证打包方式，仍早于最终 UI 与后端核验修改，不能视作最终版本。

解压目录：`验收/解压试用 空格路径/知向`。测试使用端口 8179，未操作开发目录的 8176。

- 启动前，包内不存在作者历史数据库或个人设置，试用配置存在且不会在设置响应中返回密钥。
- 把 PATH 缩减到 Windows 系统目录，并移除 PYTHONPATH/PYTHONHOME 后，直接用解压包的 `启动知向.cmd` 真实启动成功，未安装开发环境。
- 首页静态文件、12 份公开来源、明确标注的案例导读、完整原件通过 HTTP 下载均通过。这里没有进行浏览器内 PDF 预览验证。
- 导读使用 prepared 路径，不请求模型；本次没有切断操作系统网络，不将其记作物理断网测试。
- 判断保存两次、重载、两条历史和 Markdown 导出通过。
- 重复启动复用同一实例；停止后再启动形成新进程，先前判断与两条历史保留。
- 所有上述操作完成后，8179 实例已用包内停止文件正常退出。
- 具体结果见 `portable_smoke_initial.json`，仅含验收结果，不含 API Key。

## 最终 ZIP：2026-09-14 完成核验

- 文件：`交付/知向_Windows体验包.zip`。
- 字节数：40,270,475。
- SHA-256：`61abeef65c3187d51a5f9abd129844ea3363fbd27ed4f0636a1ef529b24c893f`。
- 冻结前端：`index-BBkEMaQr.js`、`index-C0osDPvb.css`；最新后端文本上下文预算修订已包含。
- 最终解压目录：`验收/最终解压验收 空格路径 20260914-120854/知向`。
- 内置 Python 3.13.15 及 7 个依赖；ssl、sqlite3、requests、pypdf、fontTools 在最终包的运行环境中导入通过。
- 12 份公开来源及原件、15 份第三方许可和说明文件已包含。612 个映射到开发目录的文件逐项 SHA-256 一致，记录见 `source_to_package_hashes.json`。运行时 `_pth` 的发行目录调整以及单独收集的许可等生成文件另行列示。
- 试用配置的模型为已真实验证的 `deepseek-flash`，密钥仅存在于包内 `config/trial.json`，不会由应用接口返回；构建报告不含密钥。
- ZIP 不带作者旧数据库、个人配置、日志、测试材料、checks、开发脚本或 node_modules；运行依赖中可剔除的 tests 目录也未打包。

最终 ZIP 的完整便携检查再次通过，详见 `portable_smoke_final.json`：

- 完整解压后初始历史为空，没有作者个人模型设置。
- PATH 仅含实际 Windows 系统目录，清除 PYTHONHOME/PYTHONPATH 后，用包内 cmd 启动成功，未安装 Docker、Node 或 Python。
- 前端页面由包内后端提供；预整理导读明确为 prepared 模式，不触发模型调用。
- HTTP 下载原件 6,882,345 字节，SHA-256 为 `2d2d788fcd1f723b910100f8ba4219c877b941752108e8b65ff15bd2395dc070`，与包内对应公开原件一致。**不将此项记作浏览器 PDF 预览已验证**。
- 判断保存两个版本、历史重载、Markdown 导出通过。
- 自填模型配置的保存、恢复试用配置通过；该检查使用明确的测试字符串，没有调用自填服务。
- 重复启动复用原进程；停止再启动形成新进程，并完整保留两条判断历史。
- 全程使用 8179，最终实例已停止，没有操作开发演示的 8176。
- 最终便携核验调用模型 0 次；现场真实模型/来源获取的验证由主任务另行记录。本次未切断操作系统网络，不声称物理断网测试或所有自填服务已验证。

`last_build.json`、`portable_smoke_final.json`、`runtime_manifest.json`、`source_to_package_hashes.json` 均重新从磁盘读取并经 JSON 解析校验，不仅依据工具输出判断成功。
