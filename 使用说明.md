# 知向联网版 · 使用说明

输入一个问题，联网寻找相关资料，核对原文，再留下自己的判断。

## 从哪里打开

将「知向联网版_Windows体验包.zip」完整解压到普通文件夹，双击「启动知向联网版.cmd」。浏览器地址为 http://127.0.0.1:8186 。内置 Windows x64 运行环境，无需安装 Docker、Python、Node.js 或本地大模型。

这是独立版本。原知向仍在原文件夹使用 8176；联网版使用自己的 data、config 和 logs，可以同时运行。联网版不会自动搬入原版个人历史。

## 第一次怎样体验

1. 在首页输入具体问题，使用联网搜索。新主题不必先有本地材料。
2. 查看候选来源的标题、网址、搜索摘要和检索时间，选中要读取的资料。
3. 读取原文后再进行 AI 分析。成功读取的正文成为本机资料，失败项会说明原因。
4. 点击来源核对，记录自己的判断和待核实问题。
5. 下次围绕同一个问题搜索新资料，加入后与已保存判断比较，再决定修改还是维持。

搜索摘要只是线索，不等于已经阅读全文。原文无法读取或日期无法确认时，不能把它当作已核验的事实。

## 搜索和模型各负责什么

- 默认使用公开 Bing RSS 搜索入口，不需要额外搜索 Key。这不是有稳定性承诺的商业搜索 API，可能无结果、结果不相关、被限流或暂时失败。
- 可在搜索设置中配置自己的 Tavily API Key；额度与服务规则取决于该账号。未配置时不会调用 Tavily。
- 新问题未手工指定搜索词时，DeepSeek 会先尝试整理最多两条简短查询；这一步也会使用模型额度。缺配置或规划失败时，仍可直接按问题进行公开搜索。DeepSeek 随后负责依据成功读取的材料整理发现、视角、缺口及新旧证据比较。联网搜索与模型分析是两个环节，模型 Key 不能当搜索服务 Key 使用。
- 课堂体验配置使用 `deepseek-v4-pro`，复用作者已授权的 DeepSeek 共享试用额度，耗尽后不自动充值，也可以填写自己的兼容 HTTPS 模型 API。2026-09-14 已完成真实年报分析核验；外部模型服务的速度与可用性仍会变化。

## 连接 AI 客户端（MCP）

本包也提供 MCP 适配器。先双击启动网页服务，再把 `MCP配置示例.json` 中的路径改成实际解压路径，导入支持 MCP 的客户端。客户端可调用 `zhixiang_search`，网页端和 MCP 端共用同一套搜索逻辑。MCP 只负责连接，不能绕过网络、模型额度或搜索服务限制；不支持 MCP 的客户端仍可直接使用网页。

## 本地资料与网络

仍可使用内置公开案例、已有资料和粘贴文字/链接。已保存资料、判断及明确标注的案例导读可以离线阅读；搜索、在线取原文和现场 AI 分析需要网络。

判断和来源保存在本目录 data 中；模型及搜索个人设置在 config 中。分析时相关问题和材料发送给所配置的模型服务，搜索时查询内容发送给搜索服务。本版不是全部计算都离线完成。

## 停止、保留与分享

关闭网页不等于退出程序。双击「停止知向联网版.cmd」关闭本目录启动的联网版；数据保留，下次可以继续。启动器不会仅凭端口或进程名强行停止其他软件。

发给同学时使用干净的原始 ZIP，不要把自己使用过的目录重新压缩，以免把个人判断或配置一起带上。保留整个解压文件夹。

## 当前限制与排错

公开搜索不保证覆盖全网、发现最新公告或补齐所有立场。搜到网址也不代表正文可读；登录页、动态网页、扫描 PDF 或超出限制的文件可能失败。本版不提供全平台抓取、实时资金流、持续后台订阅或多人同步。

AI 可能读错材料、引用不充分或夸大缺口。来源入口方便核对，不代表答案自动正确；尚无用户实验可证明消除了信息茧房或提高判断准确率。

打不开时先确认完整解压，并使用本版启动文件。网页未自动打开，可手动访问上述 8186 地址。搜索失败可尝试更具体的查询、自己的搜索配置或添加原文链接；分析失败检查模型网络与额度。启动日志在 logs/server.log，不要删除 data 排错。

本包面向 Windows x64，不提供 macOS、手机或 Windows ARM 原生安装包；其他同学电脑仍需实际验证。


